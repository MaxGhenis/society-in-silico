---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Society in Silico — The Story of Microsimulation">
  <div class="min-h-screen bg-[var(--color-paper)] relative overflow-hidden">
    <!-- Simulation grid background -->
    <div class="absolute inset-0 sim-grid"></div>

    <!-- Floating data points decoration -->
    <div class="absolute inset-0 pointer-events-none overflow-hidden">
      <div class="absolute top-[15%] left-[8%] w-2 h-2 rounded-full bg-[var(--color-sienna)] opacity-40 animate-float"></div>
      <div class="absolute top-[25%] right-[12%] w-1.5 h-1.5 rounded-full bg-[var(--color-gold)] opacity-30 animate-float delay-200"></div>
      <div class="absolute top-[60%] left-[15%] w-1 h-1 rounded-full bg-[var(--color-ink)] opacity-20 animate-float delay-400"></div>
      <div class="absolute top-[45%] right-[20%] w-2.5 h-2.5 rounded-full bg-[var(--color-sienna)] opacity-25 animate-float delay-100"></div>
      <div class="absolute bottom-[30%] left-[25%] w-1.5 h-1.5 rounded-full bg-[var(--color-gold)] opacity-35 animate-float delay-300"></div>
    </div>


    <main class="relative z-10">
      <!-- Header spacer -->
      <div class="h-8 md:h-12"></div>

      <!-- Hero -->
      <section class="px-6 md:px-12 lg:px-20 pt-12 md:pt-20 pb-16">
        <div class="max-w-6xl mx-auto">
          <div class="grid lg:grid-cols-12 gap-12 lg:gap-8 items-start">
            <!-- Left column - Main content -->
            <div class="lg:col-span-7 xl:col-span-6">
              <!-- Chapter marker -->
              <div class="animate-fade-up">
                <span class="inline-block font-mono text-[10px] tracking-[0.3em] text-[var(--color-sienna)] uppercase mb-6 border-l-2 border-[var(--color-sienna)] pl-3">
                  Forthcoming · 2026
                </span>
              </div>

              <!-- Title -->
              <h1 class="font-display text-5xl md:text-6xl lg:text-7xl font-semibold leading-[0.95] tracking-tight text-[var(--color-ink)] mb-6 animate-fade-up delay-100">
                Society<br/>
                <span class="italic font-normal">in</span> Silico
              </h1>

              <!-- Subtitle -->
              <p class="font-display text-xl md:text-2xl text-[var(--color-ink-light)] italic mb-8 animate-fade-up delay-200">
                The Story of Policy Simulation
              </p>

              <!-- Description -->
              <p class="text-lg text-[var(--color-ink-muted)] leading-relaxed mb-10 max-w-lg animate-fade-up delay-300">
                How computational models help us understand society and work toward our shared goals—from microsimulation to general equilibrium, and the AI-powered future ahead.
              </p>

              <!-- Actions -->
              <div class="animate-fade-up delay-400 flex flex-col sm:flex-row gap-4 mb-8">
                <a
                  href="/book"
                  class="inline-flex items-center justify-center gap-2 px-6 py-3 bg-[var(--color-sienna)] text-white font-mono text-sm tracking-wide hover:bg-[var(--color-sienna-light)] transition-colors"
                >
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" />
                  </svg>
                  Read Online
                </a>
                <a
                  href="#subscribe"
                  class="inline-flex items-center justify-center gap-2 px-6 py-3 bg-[var(--color-ink)] text-white font-mono text-sm tracking-wide hover:bg-[var(--color-ink-light)] transition-colors"
                >
                  Follow Updates
                </a>
              </div>

              <!-- Email signup -->
              <div class="animate-fade-up delay-500" id="subscribe">
                <form
                  action="https://buttondown.com/api/emails/embed-subscribe/maxghenis"
                  method="post"
                  target="_blank"
                  class="flex flex-col sm:flex-row gap-3 max-w-md"
                >
                  <input type="hidden" name="tag" value="society-in-silico" />
                  <input
                    type="email"
                    name="email"
                    placeholder="your@email.com"
                    required
                    class="flex-1 px-4 py-3 bg-white border border-[var(--color-paper-dark)] text-[var(--color-ink)] placeholder:text-[var(--color-ink-muted)] focus:outline-none focus:border-[var(--color-sienna)] transition-colors font-mono text-sm"
                  />
                  <button
                    type="submit"
                    class="px-6 py-3 border border-[var(--color-ink)] text-[var(--color-ink)] font-mono text-sm tracking-wide hover:bg-[var(--color-ink)] hover:text-white transition-colors"
                  >
                    Subscribe
                  </button>
                </form>
                <p class="text-xs text-[var(--color-ink-muted)] mt-3 font-mono">
                  Follow along as the book takes shape. No spam, ever.
                </p>
              </div>
            </div>

            <!-- Right column - Author & Visual -->
            <div class="lg:col-span-5 xl:col-span-6 lg:pl-8">
              <!-- Visual element - Stylized simulation representation -->
              <div class="relative mb-12 animate-fade-up delay-200">
                <div class="aspect-[4/3] bg-gradient-to-br from-[var(--color-paper-dark)] to-white border border-[var(--color-paper-dark)] relative overflow-hidden">
                  <!-- Mini grid visualization -->
                  <div class="absolute inset-4 grid grid-cols-8 grid-rows-6 gap-1">
                    {Array.from({length: 48}).map((_, i) => (
                      <div
                        class={`rounded-sm transition-all duration-500 ${
                          [3, 11, 12, 19, 20, 21, 27, 28, 35, 36, 37, 43, 44].includes(i)
                            ? 'bg-[var(--color-sienna)]'
                            : [5, 14, 22, 30, 38, 46].includes(i)
                              ? 'bg-[var(--color-gold)]'
                              : 'bg-[var(--color-ink)] opacity-5'
                        }`}
                        style={`animation-delay: ${i * 30}ms`}
                      />
                    ))}
                  </div>
                </div>
              </div>

              <!-- Author card -->
              <div class="flex items-start gap-5 animate-fade-up delay-300">
                <img
                  src="/headshot.png"
                  alt="Max Ghenis"
                  class="w-16 h-16 object-cover grayscale hover:grayscale-0 transition-all duration-500"
                />
                <div>
                  <p class="font-display text-lg font-semibold text-[var(--color-ink)]">Max Ghenis</p>
                  <div class="flex gap-4 mt-2">
                    <a href="https://twitter.com/maxghenis" class="font-mono text-[10px] tracking-wider text-[var(--color-ink-muted)] hover:text-[var(--color-sienna)] transition-colors uppercase">
                      Twitter
                    </a>
                    <a href="https://github.com/maxghenis" class="font-mono text-[10px] tracking-wider text-[var(--color-ink-muted)] hover:text-[var(--color-sienna)] transition-colors uppercase">
                      GitHub
                    </a>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Divider with quote -->
      <section class="px-6 md:px-12 lg:px-20 py-16 border-t border-[var(--color-paper-dark)]">
        <div class="max-w-4xl mx-auto text-center animate-fade-up">
          <blockquote class="font-display text-2xl md:text-3xl italic text-[var(--color-ink-light)] leading-relaxed mb-4">
            "The computer makes it possible to experiment with economic policy in something like the way a pilot can be trained in a flight simulator."
          </blockquote>
          <cite class="font-mono text-xs tracking-widest text-[var(--color-ink-muted)] uppercase not-italic">
            — Guy Orcutt, 1957
          </cite>
        </div>
      </section>

      <!-- Book structure preview -->
      <section class="px-6 md:px-12 lg:px-20 py-16 bg-white/50">
        <div class="max-w-6xl mx-auto">
          <h2 class="font-display text-2xl font-semibold text-[var(--color-ink)] text-center mb-12 animate-fade-up">
            <a href="/book" class="hover:text-[var(--color-sienna)] transition-colors">Table of Contents</a>
          </h2>
          <div class="grid md:grid-cols-3 gap-8">
            <a href="/book/birth-of-microsimulation" class="group animate-fade-up">
              <span class="font-mono text-[10px] tracking-[0.3em] text-[var(--color-sienna)] uppercase">Part I</span>
              <h3 class="font-display text-xl font-semibold text-[var(--color-ink)] mt-2 mb-3 group-hover:text-[var(--color-sienna)] transition-colors">Origins</h3>
              <p class="text-sm text-[var(--color-ink-muted)] leading-relaxed">
                From Orcutt's 1957 microsimulation to CGE models—how economists learned to simulate society.
              </p>
            </a>
            <a href="/book/policyengine-proof-of-concept" class="group animate-fade-up delay-100">
              <span class="font-mono text-[10px] tracking-[0.3em] text-[var(--color-sienna)] uppercase">Part II</span>
              <h3 class="font-display text-xl font-semibold text-[var(--color-ink)] mt-2 mb-3 group-hover:text-[var(--color-sienna)] transition-colors">Building</h3>
              <p class="text-sm text-[var(--color-ink-muted)] leading-relaxed">
                The open-source revolution—how transparent tools are democratizing policy analysis.
              </p>
            </a>
            <a href="/book/the-uncertainty-gap" class="group animate-fade-up delay-200">
              <span class="font-mono text-[10px] tracking-[0.3em] text-[var(--color-gold)] uppercase">Part III</span>
              <h3 class="font-display text-xl font-semibold text-[var(--color-ink)] mt-2 mb-3 group-hover:text-[var(--color-sienna)] transition-colors">Future</h3>
              <p class="text-sm text-[var(--color-ink-muted)] leading-relaxed">
                What happens when AI meets microsimulation? The coming transformation of policy analysis.
              </p>
            </a>
          </div>
        </div>
      </section>
    </main>

    <!-- Footer -->
    <footer class="relative z-10 px-6 md:px-12 lg:px-20 py-12 border-t border-[var(--color-paper-dark)]">
      <div class="max-w-6xl mx-auto flex flex-col md:flex-row items-center justify-between gap-4">
        <p class="font-mono text-xs text-[var(--color-ink-muted)]">
          © 2024 Max Ghenis
        </p>
        <a href="https://maxghenis.com" class="font-mono text-xs text-[var(--color-ink-muted)] hover:text-[var(--color-sienna)] transition-colors">
          maxghenis.com
        </a>
      </div>
    </footer>
  </div>
</Layout>
